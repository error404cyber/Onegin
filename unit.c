#include "unit.h"
#include "Onegin.h"
#include <assert.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>

int run_all_test(){
    assert(test_mystrcmp() == OK);
    assert(test_compar() == OK);
    assert(test_count_lines() == OK);
    assert(test_devide_lines() == OK);
    return 0;
}

Error test_compare()
  
  
Error test_
